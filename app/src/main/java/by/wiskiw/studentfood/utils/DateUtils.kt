package by.wiskiw.studentfood.utils

object DateUtils {

    fun format(timeMs: Long, format: String): String {
        // TODO formatTime(timeMs: Long, format: String): String
        return timeMs.toString()

    }
}